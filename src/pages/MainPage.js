import { Box } from '@mui/material';
import Navbar from '../components/Navbar';

function MainPage() {
    return (
        <Box>
            <Navbar/>
        </Box>
    );
}

export default MainPage;